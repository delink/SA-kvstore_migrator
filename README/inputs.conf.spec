[kvstore_migrator://<name>]
remote_uri = <value>
* This is the URL of the remote Splunk instance's REST API to pull data from.

user = <value>
* This is the username for authentication to the source_splunk REST API.

password = <value>
* This is the password for authentication to the source_splunk REST API.

app_context = <value>
* This is the app context to use when grabbing collection lists.
